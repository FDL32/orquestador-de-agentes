# Auditoria - WP-2026-156

## Focus
Validar que el review del Manager produce feedback normalizado y que el relanzado de Builder lo consume como contexto canonico, sin perder la evidencia cruda.

## Checklist de Revision

- [ ] `bus/review_bridge.py` conserva `stdout`/`stderr`/`parse_method` y genera `normalized_review` solo a partir de secciones validas.
- [ ] `scripts/manager_review_bridge.py` persiste `manager_feedback_WP-XXXX.md` por ticket.
- [ ] `scripts/launch_agent_terminals.ps1` adjunta el feedback normalizado solo en requeue y no altera el prompt base cuando no existe archivo.
- [ ] `tests/test_manager_review_bridge.py` no fuga al git del host al construir el prompt.
- [ ] `review_queue.md` y `notifications.md` siguen recibiendo un resumen auditable.
- [ ] El scope gate de `--mark-ready` sigue bloqueando cambios reales fuera de `Files Likely Touched` y no se relaja por este hotfix.

## Evidencia Esperada

- Salida limpia de:
  - `python scripts/run_pytest_safe.py tests/test_manager_review_bridge.py -q`
  - `python scripts/run_pytest_safe.py tests/test_review_bridge.py -q`
  - `python scripts/run_pytest_safe.py tests/unit/test_scope_gate.py -q`

## Criterios de Bloqueo

- Cualquier perdida de evidencia cruda del Manager.
- Cualquier requeue que no adjunte el feedback normalizado cuando exista `manager_feedback_WP-XXXX.md`.
- Cualquier test que vuelva a leer git del repo anfitrion en lugar del sandbox tmp_path.
