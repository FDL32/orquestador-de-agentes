# AUDIT_WP-2026-130 - Manager legacy naming cleanup

## Verificacion inicial
La base actual sigue mezclando la ruta legacy del Manager con nombres `codex` en varios puntos del puente de review y en tests de soporte.

## Hallazgos
- `bus/review_bridge.py` mantiene `_run_codex_review()` como nombre de la ruta legacy.
- `bus/review_bridge.py` sigue etiquetando la trazabilidad como `legacy_codex`.
- `tests/test_manager_review_bridge.py` usa fixtures y nombres de prueba basados en `codex`.
- `tests/test_launch_agent_terminals_script.py` sigue validando `templates/startup/manager_codex.md`.
- `templates/startup/manager_legacy.md` debe ser el nuevo nombre del template legacy del Manager.

## Riesgo observado
El backend `codex` real sigue definido en `agents.json` y no debe confundirse con este cleanup. El ticket solo debe limpiar la nomenclatura del Manager.

## Verificacion esperada
- La ruta legacy del Manager se renombra a una forma semantica neutra.
- El template legacy del Manager deja de presentarse con el nombre `codex`.
- Los tests reflejan la nueva nomenclatura.
- No cambia la compatibilidad del backend `codex` real.
