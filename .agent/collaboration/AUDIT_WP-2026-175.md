# Audit - WP-2026-175

## Lo que ya existe
- `scripts/session_closeout.py` ya implementa el flujo canónico de cierre.
- `.agent/agent_controller.py --session-close` ya delega en ese orquestador.
- `STATE.md`, `TURN.md` y `execution_log.md` son las proyecciones que deben sincronizarse durante el cierre.

## Riesgos
- Si el cierre no es idempotente, el siguiente ciclo puede arrastrar drift.
- Si `PROJECT.md` y `CHANGELOG.md` no se actualizan manualmente, el repositorio puede quedar con un cierre incompleto.
- Si se intenta reimplementar el cierre, el ticket duplicaría lógica ya existente.

## TP Check
- TP-01: El ticket es pequeño y acotado al cierre canónico.
- TP-02: No se toca `bus/supervisor.py`.
- TP-03: El cierre reutiliza la pipeline existente.
- TP-04: Las proyecciones de sesión quedan coherentes.
- TP-05: La validación canónica permanece verde.
