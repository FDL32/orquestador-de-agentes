# AUDIT_WP-2026-122 - Criterios de auditoria

Criterios que el Manager verificara en el review de WP-2026-122.

## Funcionales

- [ ] `runtime/project_root.py` existe y expone `resolve_project_root()`.
- [ ] Precedencia correcta: `AGENT_PROJECT_ROOT` (env) > derivacion por
      `Path(__file__)`.
- [ ] `--project-root` en los entry points exporta `AGENT_PROJECT_ROOT`.
- [ ] Los hooks leen `AGENT_PROJECT_ROOT` cuando no tienen CLI propia.

## Arquitectura

- [ ] Ningun modulo [A] mantiene una constante `PROJECT_ROOT` de import-time
      sensible al root externo; la resolucion es diferida.
- [ ] `runtime/project_root.py` es importable desde `.agent/`, `bus/`,
      `scripts/` y `runtime/`.
- [ ] No existe un segundo mecanismo de resolucion en paralelo.

## No-regresion

- [ ] Sin root externo, el comportamiento por defecto es identico al previo.
- [ ] `bus/event_bus.py`, `bus/supervisor.py`, `bus/review_bridge.py` siguen
      sin cambios internos.

## Calidad

- [ ] `ruff check .` limpio.
- [ ] `pytest` pasa, incluidos los tests nuevos de `project_root`.
- [ ] `agent_controller.py --validate` sin errores.
- [ ] El diff queda dentro de `Files Likely Touched` (scope gate literal).
