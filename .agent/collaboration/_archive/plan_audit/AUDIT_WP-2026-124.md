# AUDIT_WP-2026-124 - Criterios de revision

## Funcionales

- [ ] `inspect` dispara `STATE_CHANGED -> HUMAN_GATE` por CLI.
- [ ] `approve`, `changes` e `inspect` materializan por la misma ruta.
- [ ] `STATE.md`, `TURN.md` y `execution_log.md` reflejan la autoridad del bus.
- [ ] `--mark-ready` y `--request-changes` leen el estado derivado del bus.

## Arquitectura

- [ ] Existe una unica ruta de materializacion de transiciones.
- [ ] No hay import circular entre `bus/review_bridge.py` y `agent_controller.py`.
- [ ] No hay auto-healing silencioso de drift.
- [ ] La documentacion describe el bus como autoridad canonica.

## Calidad

- [ ] `ruff check .` limpio.
- [ ] `pytest` pasa en el slice del bus y review bridge.
- [ ] `agent_controller.py --validate` limpio.
- [ ] `test_emit_fail_safe_on_review_decision` pasa.
- [ ] El test e2e `tests/test_review_cycle_e2e.py` pasa.

## No regresion

- [ ] El flujo de review sigue funcionando para `approve`, `changes` e
      `inspect`.
- [ ] No se rompe el cierre canonico de tickets ya resueltos.
- [ ] Las proyecciones siguen siendo solo snapshots derivados del bus.
