# AUDIT_WP-2026-135 - Selective context recovery lite for pre-compact hook

## Verificacion inicial
`pre_compact_hook.py` esta hoy en modo stub: lee stdin, devuelve `continue=true` y no proyecta memoria relevante. El cambio buscado es pequeno, pero debe ser robusto frente a memoria ausente o corrupta.

## Hallazgos
- El hook actual no lee `observations.jsonl`.
- No existe una proyeccion de `Memoria relevante`.
- No hay ranking de observaciones por recencia o keywords.
- No hay cobertura de tests para el hook.
- No se especifica aun el campo JSON `additionalContext`, que es el canal correcto para que Claude Code reciba la proyeccion antes de compactar.
- No se especifica la resolucion de `AGENT_DIR` desde `Path(__file__).resolve().parent.parent`, asi que un uso de `Path.cwd()` seria fragil.

## Riesgos a acotar
- Si el hook intenta escribir `STATE.md` directamente, puede introducir drift con la proyeccion canonica del controlador.
- Si la lectura de memoria falla de forma no controlada, la compactacion podria bloquearse en sesiones largas.
- Si se intenta usar LLM o dependencias pesadas, el ticket deja de ser ligero y pierde el valor de recuperacion determinista.

## Verificacion esperada
- El hook sigue devolviendo JSON valido con `continue=true`.
- La memoria relevante se proyecta como seccion compacta y acotada a 5 entradas dentro de `additionalContext`.
- El ranking prioriza observaciones recientes y alineadas con el `work_plan` activo.
- Los tests cubren memoria vacia, archivo ausente, JSONL corrupto, matching basico y presencia de `additionalContext`.
