# Audit - WP-2026-168

## Metadata
- **ID:** WP-2026-168
- **Estado:** APPROVED
- **deliverable_type:** code
- **Titulo:** Session closeout orchestrator - audit, memory, archive

## TP Check
- TP-01: verificado - las 4 fases son secuenciales sin contradiccion; Fase 1 resuelve el alcance, Fase 2 encadena calidad, Fase 3 memoria/archivado y Fase 4 portabilidad/reporte.
- TP-02: verificado - los criterios citan `python scripts/prepush_check.py`, `python scripts/run_pytest_safe.py tests/test_session_closeout.py`, `uv run ruff check scripts/session_closeout.py tests/test_session_closeout.py`, `python scripts/session_closeout.py --project-root . --dry-run`, `python scripts/validate_ticket_prose.py --json` y `python .agent/agent_controller.py --validate --json --force`.
- TP-03: verificado - Files Likely Touched lista archivos concretos sin comodines.
- TP-04: verificado - no aparece lenguaje vago en el flujo critico; first-run fallback, `--skip-slow`, rutas absolutas y recomendaciones manuales estan definidos.
- TP-05: verificado - PLAN y AUDIT describen las mismas 4 fases, los mismos archivos y los mismos criterios observables.

## Criterios de Aceptacion Global
- El orquestador resuelve el rango de sesion de forma determinista, incluso en la primera ejecucion.
- `--skip-slow` omite memoria/observaciones pero mantiene el cierre utilizable.
- `prepush_check.py` bloquea el cierre si la calidad no pasa.
- `validate_ticket_prose.py --json` valida los tickets tocados durante la sesion.
- `.agent/runtime/memory/session_close_report.md` resume auditoria, calidad, memoria, archivado y portabilidad con PASS/WARN/FAIL.
- El script no ejecuta skills como si fueran binarios; solo orquesta scripts reales.
- `git status --short` queda limpio al final del cierre automatizado salvo por el reporte runtime esperado en `.agent/runtime/memory/session_close_report.md`.

## Blockers
1. Sin una politica clara de rango de sesion, el cierre no sabe que tickets auditar.
2. Sin `--skip-slow`, el cierre puede volverse demasiado pesado para sesiones largas o urgentes.
3. Si el script intenta ejecutar skills de prosa como comandos, el flujo falla por diseno.
4. Sin verificacion concreta de `MANIFEST.distribute` y de rutas absolutas, la portabilidad queda solo declarativa.
5. Si el cierre bloquea por pasos informativos, se pierde utilidad operacional.

## Evidencia Esperada
- `scripts/session_closeout.py` con CLI propia, reporte y orquestacion de scripts reales.
- `tests/test_session_closeout.py` con cobertura de rango de sesion, `--skip-slow`, first-run fallback, portabilidad y reporte.
- `.agent/runtime/memory/session_close_report.md` generado por ejecucion.
- El reporte deja claro que `code-audit` y `bui-self-audit` son recomendaciones manuales, no comandos ejecutados.
