# AUDIT_WP-2026-154 - Strictness profiles and live guard_paths activation

## Review Focus
This review checks whether the hook is actually live, whether the new profile schema is backward compatible, and whether the strict mode improves safety without breaking canonical collaboration files.

## Must-pass criteria
- `guard_paths.py` main path executes real guard logic instead of unconditional `continue`.
- `guard_paths.py` reports block reasons on `stderr` and uses hook exit codes instead of JSON output as the contract.
- `guard_paths.py` loads `agents.json` directly and does not depend on `agents_config.py` for profile selection.
- `agents.json` accepts `strictness_profile` and a profile map, with `standard` as the safe default.
- Migration 1.1 -> 1.2 backfills `strictness_profile = standard` without corrupting existing config.
- `minimal`, `standard`, and `strict` produce different enforcement levels.
- `pytest --collect-only` on the two target tests passes before execution.
- `execution_log.md` and other live canonical surfaces remain usable and are not globally blocked by strict mode.

## Files to inspect
- `.agent/hooks/guard_paths.py`
- `.agent/agents_config.py`
- `.agent/config/agents.json`
- `tests/conftest.py`
- `tests/test_guard_paths.py`
- `tests/unit/test_agents_config.py`

## Review Questions
1. Does `__main__` in `guard_paths.py` now invoke the real checks?
2. Does the new schema fail closed on invalid profile values?
3. Does the migration preserve older configs?
4. Do the tests prove the three profile behaviors and the hook output?
5. Is the live-state surface preserved for canonical collaboration files?
6. Does test collection stay clean before execution?

## Approval bar
- Approve only if the change is functional, backwards compatible, and covered by tests.
- If the hook still behaves like a stub, request changes.
- If strict mode blocks canonical live state files, request changes.
