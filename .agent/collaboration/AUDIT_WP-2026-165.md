# Audit - WP-2026-165

## Metadata
- **ID:** WP-2026-165
- **Estado:** READY_FOR_REVIEW
- **deliverable_type:** mixed
- **Titulo:** Delivery preflight wrapper - canonical push readiness

## TP Check
- TP-01: verificado - las 3 fases son secuenciales sin contradiccion; Fase 1 crea el wrapper, Fase 2 crea la cobertura y Fase 3 actualiza la documentacion.
- TP-02: verificado - los criterios citan `python scripts/prepush_check.py`, `python scripts/validate_ticket_prose.py`, `python -m pytest tests/test_validate_ticket_prose.py -q`, `python skills/validate_all.py` y `python .agent/agent_controller.py --validate --json --force`.
- TP-03: verificado - Files Likely Touched lista 9 archivos concretos sin comodines.
- TP-04: verificado - no aparece lenguaje vago en el flujo critico; el comportamiento por defecto, la ruta de reparacion inexistente y el aprendizaje TP-07 recuperado quedan descritos de forma literal.
- TP-05: verificado - PLAN y AUDIT describen las mismas 3 fases, los mismos 9 archivos y los mismos criterios observables.

## Criterios de Aceptacion Global
- El comando canonico de preflight existe y puede ejecutarse antes de `git push`.
- El modo por defecto del wrapper es no mutador.
- Si se incluye modo de reparacion, este corre la pasada mutadora de forma explicita y luego vuelve a confirmar que el arbol queda limpio.
- La documentacion del ciclo de delivery apunta al mismo comando unico en lugar de una lista manual dispersa.
- El preflight detecta antes del push los fallos de hooks, tree dirty, ruff, validacion de tickets y validacion del controller.

## Blockers
1. Si el wrapper no distingue claramente entre verificacion pura y reparacion explicita, el flujo puede volver a mutar en el momento equivocado.
2. Si la ruta limpia no informa del estado del arbol con `git status --short`, el diagnostico de push recupera ambiguedad.
3. Si la documentacion del ciclo de delivery no referencia el mismo comando canonico, el operador seguira teniendo que reconstruir la rutina a mano.

## Evidencia Esperada
- `scripts/prepush_check.py` con CLI utilizable y diagnostico legible.
- `tests/test_prepush_check.py` cubriendo limpio, sucio y reparacion/confirmacion.
- `scripts/validate_ticket_prose.py` y `tests/test_validate_ticket_prose.py` con TP-07 / TP-PROSE-12 recuperados.
- `skills/_shared/ticket-anti-patterns.md` y `skills/man-create-work-plan/references/plan-quality-checklist.md` con el aprendizaje de alcance condicional formalizado.
- `skills/project-finalize/SKILL.md` mencionando el comando canonico del preflight.
- `PROJECT.md` y `QUICKSTART.md` reflejando el flujo de entrega con una unica rutina.
