# AUDIT_WP-2026-123 - Criterios de revision

## Funcionales

- [ ] El instalador crea en el destino solo el `.agent/` minimo necesario.
- [ ] Existe un archivo de enlace motor-destino en `.agent/config/`.
- [ ] El schema del enlace esta definido en la documentacion del ticket.
- [ ] `active_profile` del destino queda en `host-project`.
- [ ] El motor operativo no se copia al destino.

## Arquitectura

- [ ] `MANIFEST.workspace` sigue definiendo la allowlist del destino.
- [ ] `MANIFEST.workspace` conserva el link generado para que sobreviva a
      `--sync --prune`.
- [ ] No aparece un segundo mecanismo de copia / sincronizacion en paralelo.
- [ ] La documentacion describe claramente motor central vs workspace destino.
- [ ] Queda claro que lo nuevo de WP-123 es el enlace y su conservacion.

## Calidad

- [ ] `ruff check .` limpio.
- [ ] `pytest` pasa en el slice del instalador.
- [ ] `agent_controller.py --validate` limpio.

## No regresion

- [ ] El flujo legacy de instalacion / sync sigue siendo idempotente.
- [ ] El destino conserva solo estado, memoria, eventos y configuracion.
- [ ] No se introducen cambios internos en `bus/event_bus.py`, `bus/supervisor.py`
      ni `bus/review_bridge.py`.
