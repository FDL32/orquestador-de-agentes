# Work Plan - WP-2026-156

## Metadata
- **ID:** WP-2026-156
- **Estado:** IN_PLANNING
- **deliverable_type:** code
- **Titulo:** Manager feedback normalization and Builder relaunch handoff
- **Asignado a:** Builder

## Objetivo
Separar el feedback crudo del Manager del artefacto canonico legible, persistir un resumen normalizado por ticket y reinyectarlo al relanzar Builder tras `CHANGES`.

## Contexto
El review transport actual ya detecta `CHANGES`, pero el Builder necesita un artefacto legible y estable para reanudar el trabajo sin depender del stream bruto del backend. Este ticket tambien corrige la fuga de git del test de bridge para que el sandbox no lea el repo anfitrion.

## Estado Actual
- `_record_review()` en `scripts/manager_review_bridge.py` ya escribe `manager_feedback_WP-XXXX.md`; el cambio real es el contenido, no el mecanismo.
- `tests/unit/test_scope_gate.py` ya existe y pasa; no hace falta crearlo ni reubicarlo.
- `scripts/launch_agent_terminals.ps1` ya soporta el arreglo de canonicals con `-f`; la inyeccion debe reutilizar ese mecanismo.
- El delta real de este ticket es normalizar `feedback`, preservar `stdout` crudo y reinyectar el archivo canonico solo en requeue.

## Decision Arquitectonica
- `raw_review` se conserva como evidencia, nunca como input directo del Builder.
- `normalized_review` se escribe en `.agent/collaboration/manager_feedback_WP-XXXX.md`.
- El relanzado de Builder adjunta el feedback normalizado al prompt solo cuando existe un `CHANGES` previo.
- El scope gate de `--mark-ready` sigue siendo estricto; los artefactos auto-generados del sistema siguen excluidos por el controlador.
- Los tests de `manager_review_bridge` deben aislar git para no escapar del sandbox tmp_path.

## Fases
### Fase 0: Normalizar el feedback de review
- **Tipo:** TAREA AGENTE
- **Archivo:** `bus/review_bridge.py`
- **Accion:** Modificar
- **Descripcion:** Construir `normalized_review` desde `ReviewResult.stdout` (stream NDJSON de OpenCode) extrayendo el ultimo bloque de texto legible tras los eventos `step_finish`, limpiando ANSI codes y preservando el bloque `DECISION:` y cualquier bloque `BLOCKERS/CHANGES`. `ReviewResult.stdout` permanece intacto como evidencia cruda.
- **Riesgo:** Medio
- **Criterio de Aceptacion:** `ReviewResult.feedback` queda normalizado y legible; `ReviewResult.stdout`, `stderr` y `parse_method` siguen preservando el raw stream sin tocar.
- **Si falla:** Restaurar `feedback` a salida legible completa y mover la normalizacion a una capa separada.

### Fase 1: Mejorar el contenido de `_record_review()`
- **Tipo:** TAREA AGENTE
- **Archivo:** `scripts/manager_review_bridge.py`
- **Accion:** Modificar
- **Descripcion:** Reusar el mecanismo existente de escritura para que `manager_feedback_WP-XXXX.md` reciba `feedback` normalizado, mientras `raw_stdout` y `raw_stderr` quedan en el bloque `Raw Review` como evidencia cruda.
- **Riesgo:** Medio
- **Criterio de Aceptacion:** El archivo por ticket contiene `decision`, `parse_method`, `feedback` normalizado y un bloque `Raw Review` con el stream crudo.
- **Si falla:** Revertir el mapeo de `feedback` y dejar `raw_stdout`/`raw_stderr` solo como evidencia.

### Fase 2: Inyectar feedback en el relanzado de Builder
- **Tipo:** TAREA AGENTE
- **Archivo:** `scripts/launch_agent_terminals.ps1`
- **Accion:** Modificar
- **Descripcion:** Cuando exista `manager_feedback_WP-XXXX.md`, agregarlo al array de canonicals con `-f` antes de lanzar Builder en requeue; no concatenar el contenido al prompt base.
- **Riesgo:** Alto
- **Criterio de Aceptacion:** Builder relanzado recibe el feedback normalizado sin alterar el prompt base cuando no hay CHANGES.
- **Si falla:** Quitar la inyeccion y mantener el archivo solo para auditoria.

### Fase 3: Aislar el test de bridge del git del host
- **Tipo:** TAREA AGENTE
- **Archivo:** `tests/test_manager_review_bridge.py`
- **Accion:** Modificar
- **Descripcion:** Aislar `_git_diff_stat()` y `_build_diff_for_files_likely_touched()` para que no escapen al git del repositorio anfitrion cuando el test corre en tmp_path; si hace falta, stubear solo esas rutas.
- **Riesgo:** Medio
- **Criterio de Aceptacion:** Los tests del bridge solo leen el sandbox del test y no arrastran artefactos externos.
- **Si falla:** Aislar los helpers de git con monkeypatch o stubs de alta fidelidad.

### Fase 4: Validacion E2E del handoff CHANGES → requeue → relaunch
- **Tipo:** TAREA AGENTE
- **Archivo:** `tests/test_manager_review_bridge.py`, `tests/test_review_bridge.py`
- **Accion:** Verificar
- **Descripcion:** Verificar el flujo completo extremo a extremo, distinto del aislamiento de git de Fase 3: (1) decision CHANGES produce `feedback` normalizado en `ReviewResult`; (2) `_record_review()` persiste ese feedback en `manager_feedback_WP-XXXX.md`; (3) el launcher detecta el archivo y lo adjunta via `-f` al relaunch de Builder. La suite de `test_review_bridge.py` debe confirmar ademas que `ReviewResult.stdout` permanece intacto tras la normalizacion.
- **Riesgo:** Bajo
- **Criterio de Aceptacion:** Las tres suites pasan (`test_manager_review_bridge.py`, `test_review_bridge.py`, `tests/unit/test_scope_gate.py`) y ninguna lectura cruza el sandbox hacia el repo anfitrion.
- **Si falla:** Ajustar el parser de secciones (Fase 0), el writer de feedback (Fase 1) o el punto de inyeccion del launcher (Fase 2) segun donde falle la cadena.

## Files Likely Touched
- `bus/review_bridge.py`
- `scripts/manager_review_bridge.py`
- `scripts/launch_agent_terminals.ps1`
- `tests/test_manager_review_bridge.py`
- `tests/test_review_bridge.py`

## Criterios de Aceptacion Global
- [ ] `raw_review` y `normalized_review` quedan separados por contrato.
- [ ] `ReviewResult.stdout` permanece intacto tras la normalizacion.
- [ ] El artefacto `.agent/collaboration/manager_feedback_WP-XXXX.md` se crea por ticket con metadatos utiles.
- [ ] Builder relanzado consume el feedback normalizado cuando el Manager emite `CHANGES`.
- [ ] Los tests del bridge no escapan del sandbox y no leen el repo anfitrion.
