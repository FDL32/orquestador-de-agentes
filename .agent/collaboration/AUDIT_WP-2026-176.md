# Audit - WP-2026-176

## Lo que ya existe
- `AGENT_PROJECT_ROOT` env var y `--project-root` CLI ya permiten separar workspace de codigo del motor.
- `scripts/install_agent_system.py::write_motor_destination_link()` ya genera el nexo motor/destino.
- `runtime/project_root.py` ya tiene infraestructura para resolver rutas de workspace externo.

## Riesgos
- Guard anti-drift activado antes de migración puede bloquear operaciones legítimas en el motor — mitigado con `--project-root` explícito.
- Migración física (Fase 4) es destructiva: backup previo obligatorio antes de cualquier movimiento.
- `is_motor_code_only()` usa detección heurística (presencia de `agent_controller.py`) en lugar de marcador explícito — puede dar falsos positivos en entornos de test.
- El sync del instalador copia superficies de template, no del estado activo — el workspace destino puede arrancar con tickets stale.

## TP Check
- TP-01: Ticket sobredimensionado (11 archivos) — justificado para una migración de workspace/frontera de motor.
- TP-02: No se toca `bus/supervisor.py`.
- TP-03: El guard anti-drift usa el mecanismo existente de `AGENT_PROJECT_ROOT`, sin nueva infraestructura.
- TP-04: Tests cubren guard (bloqueo antes de I/O), bootstrap con `--project-root`, y resolución de controller externo.
- TP-05: Verificación final: `ruff` limpio, `381 passed`, `--validate` 0 errores, workspace destino con config correcta.
